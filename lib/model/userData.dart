import 'package:cirs_app/model/complexity_data.dart';
import 'package:cirs_app/model/score_data.dart';

class UserData {
  ///creates a list used to store all data used in calculating the complexity score of an incident
  static List<ScoreData> myScoreData = [];

  ///creates a list used to store all data used in determining the complexity of an incident.
  static List<ComplexityData> myComplexityData = [];



}
